@using Application.Game.Inventory
@using Radzen

<RadzenGrid AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" AllowPaging="true" PageSize="4"
            AllowSorting="true" Data="@StorageHandler?.GetList()" TItem="StorageItem">
	<Columns>
		<RadzenGridColumn TItem="StorageItem" Property="Item.Name" Title="Name"/>
		@* <RadzenGridColumn TItem="C" Property="Count" Title="Quantity"/> *@
	</Columns>
</RadzenGrid>

@code
{
	[Parameter]
	public StorageHandler? StorageHandler { get; set; }
}